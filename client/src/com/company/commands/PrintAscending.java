package com.company.commands;

import com.company.serv.Connect;
import com.company.serv.Pak;

/*public class PrintAscending implements Command { //вывести элементы коллекции в порядке возрастания
    @Override
    public String run(String s) {

        if (s.contains("print_ascending") || s.contains("print ascending")) {
            Pak pak = new Pak();

            pak.type = CommandType.PRINT_ASCENDING;
            Connect.send(pak);

            return "";
        }
        return "";
    }
}*/

